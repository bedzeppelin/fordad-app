name: Push notification cron

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Call /api/push/cron
        run: |
          curl -sS -X GET "https://fordad-app.vercel.app/api/push/cron" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            --write-out "\nHTTP status: %{http_code}" \
            --fail-with-body
